{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBundleCommand": "scripts/cef-helpers.sh src-tauri/target/cef-helpers"
  },
  "bundle": {
    "macOS": {
      "files": {
        "Frameworks/hz Helper.app": "target/cef-helpers/hz Helper.app",
        "Frameworks/hz Helper (GPU).app": "target/cef-helpers/hz Helper (GPU).app",
        "Frameworks/hz Helper (Renderer).app": "target/cef-helpers/hz Helper (Renderer).app",
        "Frameworks/hz Helper (Plugin).app": "target/cef-helpers/hz Helper (Plugin).app",
        "Frameworks/hz Helper (Alerts).app": "target/cef-helpers/hz Helper (Alerts).app"
      }
    }
  }
}
